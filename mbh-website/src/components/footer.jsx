import { useNavigate, useLocation } from "react-router-dom";
import { useEffect, useRef } from "react";

function Footer() {
  return <div className="w-full flex flex-col">hi</div>;
}

export default Footer;
